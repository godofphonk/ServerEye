version: '3.8'

services:
  # PostgreSQL for registered keys
  postgres-keys:
    image: postgres:15-alpine
    container_name: servereye-postgres-keys
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: PgRegisteredKeys
      POSTGRES_USER: servereye_keys
      POSTGRES_PASSWORD: ${KEYS_DB_PASSWORD:-KMRb0xHxWCH%2FQa28YskBl62xI%2FBfkwi%2FZPiHMrZueEc%3D}
      POSTGRES_INITDB_ARGS: "--auth-host=md5"
      POSTGRES_HOST_AUTH_METHOD: md5
    volumes:
      - postgres_keys_data:/var/lib/postgresql/data
      - ./deployments/init-keys-db.sql:/docker-entrypoint-initdb.d/init-schema.sql
    networks:
      - servereye-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U servereye_keys"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Backend API
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: servereye-backend-prod
    network_mode: host
    environment:
      - DATABASE_URL=${KEYS_DATABASE_URL:-postgres://servereye_keys:KMRb0xHxWCH%2FQa28YskBl62xI%2FBfkwi%2FZPiHMrZueEc%3D@localhost:5432/PgRegisteredKeys?sslmode=disable}
      - KEYS_DATABASE_URL=${KEYS_DATABASE_URL:-postgres://servereye_keys:KMRb0xHxWCH%2FQa28YskBl62xI%2FBfkwi%2FZPiHMrZueEc%3D@localhost:5432/PgRegisteredKeys?sslmode=disable}
      - API_KEY=${API_KEY:-sPnMkMxyxIcjq1kJD7FOtEjUrHxvSmEU}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres_keys_data:

networks:
  servereye-network:
    driver: bridge
