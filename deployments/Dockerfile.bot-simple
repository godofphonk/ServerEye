FROM alpine:latest

# Install ca-certificates for HTTPS
RUN apk --no-cache add ca-certificates

# Create app directory
WORKDIR /app

# Copy the binary
COPY downloads/servereye-bot-linux /app/bot

# Make it executable
RUN chmod +x /app/bot

# Expose port
EXPOSE 8080

# Run the bot
CMD ["/app/bot"]
