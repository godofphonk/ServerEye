# ServerEye DEV Environment Configuration
# Copy this file to .env.dev and fill in your credentials

# Telegram Bot Token for DEV bot (get from @BotFather)
TELEGRAM_TOKEN_DEV=your_dev_bot_token_here

# Database Password (generate with: openssl rand -base64 32)
DB_PASSWORD=your_strong_db_password_here

# Redis Password (generate with: openssl rand -base64 32)
REDIS_PASSWORD=your_strong_redis_password_here

# GitHub Token for private Docker images (same as PROD, generate with read:packages permission)
GITHUB_TOKEN=your_github_token_here

# Watchtower Notifications (optional)
# WATCHTOWER_NOTIFICATIONS=
# WATCHTOWER_NOTIFICATION_URL=
