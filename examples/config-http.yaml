server:
  name: "servereye-agent"
  description: "ServerEye agent with HTTP API"
  secret_key: "srv_91a1a72ea96869c28b595996a8c47e09"

# HTTP API configuration (alternative to Kafka)
api:
  base_url: "http://localhost:8080"
  api_key: "prod-api-key-2024-change-me"
  timeout: "30"

# Kafka configuration (disabled when using HTTP API)
kafka:
  enabled: false
  brokers: []
  topic_prefix: ""

metrics:
  cpu_usage: false
  memory_usage: false
  disk_usage: false
  cpu_temperature: true
  interval: "30s"

logging:
  level: "INFO"
  file: ""
